# waypoint_selector
This repo is used for selection of waypoints for the user interface

It opens a map interface where the user can select points to be pasted into the orpheus vehicle user interface.
